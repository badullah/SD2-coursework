extends layout
block content
    - console.log(data)
    .main-content 
    style.
        //- h1 {text-align:center;}
        //- table.ui.celled img { display: inline-block; }
        table, th, td {
        border: 1px solid black;
        border-collapse: collapse;
        }
        footer { margin: 35px 0 15px 0; text-align: left }
    h1 All Users
    table
        tr
        th User ID
        th Name
        th Email
        for item in data 
            tr  
                td #{item.id}
                td 
                    a(href='/user-single/' + item.id) #{item.name} 
                td #{item.email}