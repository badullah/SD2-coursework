extends layout
block content
    h1 Guest page for: #{guest.name}
    p Guest ID: #{guest.id} 
    h2 List Name: #{guest.list.lName}
    p List Items 
    table(border=1)
        for item in guest.items 
            tr  
                td #{item.id}
                td #{item.item_name}
    