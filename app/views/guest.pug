extends layout
block content
    h1 Guest page for: #{guest.name}
    p Guest ID: #{guest.id} 
    p List Name: #{guest.list.lName}
    //- h2 Items 
    //- table(border=1)
    //-     for item in guest.items
    //-         tr  
    //-             td #{item.code}
    //-             td 
    //-                 a(href='/single-item/' + item.code) #{item.iName}
    